- name: "install node.js"
  become: true
  shell: |
    curl -SL https://deb.nodesource.com/setup_19.x | sudo -E bash - &&\
- name: "install node.js"
  become: true
  apt:
    name: ["nodejs"]
    state: latest
    update_cache: yes

- name: "install pm2"
  npm:
    name: pm2
    global: yes
